// Export all icons for backward compatibility
// Default 24px icons for backward compatibility
export { Star } from './24';

// Export base Icon component (default to 24px)
export { Icon24 as Icon } from '../components/Icon24';

// Export types for convenience
export type { IconProps } from '../types';
